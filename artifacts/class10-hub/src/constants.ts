const currentYear = new Date().getFullYear();
const nextYear = currentYear + 1;

// Ye apne aap "2026-2027" ya agle saal "2027-2028" bana dega
export const CURRENT_SESSION = `${currentYear}-${nextYear}`;

